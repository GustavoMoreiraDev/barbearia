const Header = () => {
    return (
        <header>
            <span className="menu-header">
                <a href="#">Inicio</a>
                <a href="#">Sobre</a>
                <a href="#">Contato</a>
            </span>
        </header>
    )
}

export default Header;